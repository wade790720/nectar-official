# 本地開發：不要設或設 false — 作品存 localStorage
# 正式 build（import.meta.env.PROD）預設會走 R2 + /api/*，可不設本項
# 若要本機也打遠端 API：VITE_USE_REMOTE=true
# 若要強制正式站只存瀏覽器（不建議）：VITE_USE_REMOTE=false
# VITE_USE_REMOTE=
#
# 與 Cloudflare Pages Secret「ADMIN_SECRET」需一致。
# 正式站請勿設定下一行：管理員在網站上用「Admin」登入即可，避免金鑰被打進前端 bundle。
# 僅本地開發可選：略過登入（import.meta.env.DEV 時有效）
# VITE_ADMIN_KEY=你的長亂碼
#
# 僅在 API 與網站不同網域時要填，例如自訂 API 網域；同域留空
# VITE_API_BASE=
