# ── 本機連正式 R2（接近 prod）──
# 1) 設 VITE_USE_REMOTE=true
# 2) 設 VITE_DEV_PROXY_TARGET=https://你的專案.pages.dev（不要結尾斜線）
#    → npm run dev 時 /api 會由 Vite 轉發到該網址，與線上同一桶 R2，且無 CORS 問題
# 3) Admin 登入密碼 = Cloudflare ADMIN_SECRET
#
# 若不用 proxy、改把 API 網域寫進 VITE_API_BASE，須部署含 functions/_middleware.js 的版本（localhost CORS）
#
# 本地純離線（預設）：不要設 VITE_USE_REMOTE 或設 false
# 正式 build 預設會走 R2；若要強制瀏覽器 only：VITE_USE_REMOTE=false
# VITE_USE_REMOTE=true
# VITE_DEV_PROXY_TARGET=https://nectar-official.pages.dev
#
# 與 Cloudflare Pages Secret「ADMIN_SECRET」需一致。
# 正式站請勿設定下一行：避免金鑰被打進前端 bundle；後台請用右下角隱藏區或 #nectar-admin。
# 僅本地開發可選：略過登入（import.meta.env.DEV 時有效）
# VITE_ADMIN_KEY=你的長亂碼
#
# 僅在 API 與網站不同網域時要填，例如自訂 API 網域；同域留空
# VITE_API_BASE=
#
# 頁尾社群連結（完整網址或網域，例如 instagram.com/yourpage）
# VITE_SOCIAL_INSTAGRAM=
# VITE_SOCIAL_FACEBOOK=
#
# 頁尾社群列「聯絡 / Contact」mailto（僅填信箱，不要加 mailto:）
# VITE_CONTACT_EMAIL=hello@example.com
#
# 課程頁外部報名連結（台北 / 台中）
# VITE_COURSE_TAIPEI_URL=
# VITE_COURSE_TAICHUNG_URL=
# 課程學院首頁
# VITE_COURSE_MAIN_URL=
#
# 後台登入（不對外顯示按鈕）：① 點螢幕右下角隱藏角落約 56×56px ② 或網址列加上 #nectar-admin
