# 本地開發：不要設 VITE_USE_REMOTE，或設為 false — 作品仍存 localStorage
# 正式佈署（Cloudflare build）：設為 true，圖與作品 JSON 走 R2 + Pages Functions
# VITE_USE_REMOTE=true
#
# 與 Cloudflare Pages Secret「ADMIN_SECRET」需一致（寫入 / 上傳用）
# VITE_ADMIN_KEY=你的長亂碼
#
# 僅在 API 與網站不同網域時要填，例如自訂 API 網域；同域留空
# VITE_API_BASE=
